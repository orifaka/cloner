# Optional intro media

Place one of these files to show on `/start`:

- `intro.mp4` (recommended, short 5–10s loop)
- `intro.gif`
- `intro.jpg` / `intro.png`

Path can be overridden with `INTRO_MEDIA_PATH` in `.env`.

If no file is present, text-only premium intro is used.
